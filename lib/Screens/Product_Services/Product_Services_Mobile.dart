import 'package:flutter/material.dart';
import '../../Widgets/product_services_cards/cards.dart';

class ProductServicesMobile extends StatelessWidget {
  const ProductServicesMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
